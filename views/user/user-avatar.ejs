<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>تغییر آواتار</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <%- include('../partials/dashboard/scripts-header') %>
    <link rel="stylesheet" href="/stylesheets/dashboard/addPicture.css">
  </head>
  <body class="hold-transition sidebar-mini">
    <div class="wrapper">
      <%- include('../partials/dashboard/navbar') %> <%-
      include('../partials/dashboard/sidebar') %>

      <div class="content-wrapper">
        <section class="content-header">
          <div class="container-fluid">
            <div class="row mb-2">
              <div class="col-sm-6">
                <h3>تغییر آواتار</h3>
              </div>
              <div class="col-sm-6">
                <ol class="breadcrumb float-sm-left d-none d-lg-flex">
                  <li class="breadcrumb-item">
                    <a href="/user/dashboard">داشبورد</a>
                  </li>
                  <li class="breadcrumb-item active">تغییر آواتار</li>
                </ol>
              </div>
            </div>
          </div>
        </section>

        <section class="content">
          <% if (avatar) { %>
            <div
              class="col-10 col-lg-6 alert alert-success alert-dismissible fade show w-75 text-right mx-auto"
              role="alert"
            >
              <button
                type="button"
                class="close"
                data-dismiss="alert"
                aria-label="Close"
              >
                <span aria-hidden="true">&times;</span>
              </button>
              <%= avatar %>
            </div>
            <% } %>
          <div class=" col-12 text-center m-auto">
            <div class="mt-5">
              <form
                id="uploadForm"
                enctype="multipart/form-data" action="/user/uploadAvatar?_method=PUT"
                method="post"
              >
              <input type="hidden" name="_method" value="PUT" />
                  <div class="file-input col-sm-10">
                    <input type="file" name="avatar" required />
                    <span class='button'>انتخاب تصویر</span>
                    <span class='label' data-js-label>تصویری انتخاب نشده است</label>
                  </div>
                  <input
                  type="submit"
                  value="بارگذاری"
                  name="submit"
                  class="btn btn-success mt-4 d-none"
                  id="avatarSubmit"
                /> 
              </form>
            </div>
            <div class="col-12">
              <form action="/user/removeAvatar?_method=DELETE" method="post">
                <input type="hidden" name="_method" value="DELETE" />
                <input type="text" class="d-none" value="default" name="default">
                <input type="submit" class="d-none btn btn-danger text-white mt-5" id="removeSubmit" value="حذف آواتار"> 
              </form>
            </div>
          </div>
          <div class="mx-auto col-12 text-center mt-5">
            <button class="btn btn-success ml-5" id="uploadAvatar">بارگذاری</button><button class="btn btn-danger" id="removeAvatar">حذف آواتار</button>
          </div>
        </section>
      </div>

      <%- include('../partials/dashboard/footer') %>

      <aside
        class="control-sidebar control-sidebar-dark control-sidebar-animate"
      ></aside>
    </div>
    <%- include('../partials/dashboard/scripts-footer') %>
    <script src="/javascripts/dashboard/addPicture.js"></script>
    <script>
      $(document).ready(()=>{
        $("#removeAvatar").click(()=>{
          $("#removeSubmit").click();
        })
        $("#uploadAvatar").click(()=>{
          $("#avatarSubmit").click();
        })
      })
    </script>
  </body>
</html>
